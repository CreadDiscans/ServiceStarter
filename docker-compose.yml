version: '3'

services:
  prod:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app
      - ./nginx.conf:/etc/nginx/sites-enabled/nginx.conf
      - ./uwsgi.ini:/etc/uwsgi/apps-enabled/uwsgi.ini
      - ./log:/var/log/nginx
      - ./log:/var/log/uwsgi/app
      # - /etc/letsencrpt:/etc/letsencrypt
    ports:
      - "80:80"
      - "443:443"
    command:
      - python3 
      - manage.py
      - build